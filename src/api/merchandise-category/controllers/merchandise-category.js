'use strict';

/**
 * merchandise-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::merchandise-category.merchandise-category');
