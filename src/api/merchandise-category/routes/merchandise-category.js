'use strict';

/**
 * merchandise-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::merchandise-category.merchandise-category');
