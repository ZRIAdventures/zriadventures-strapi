/**
 * Tour Booking Lifecycle Hooks
 *
 * DISABLED: All booking logic moved to frontend checkout API route
 * to avoid Strapi v5 Document API compatibility issues
 */

module.exports = {
  // All hooks disabled - logic handled in frontend
};
