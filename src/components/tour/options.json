{
  "collectionName": "components_tour_options",
  "info": {
    "displayName": "tourOptions",
    "icon": "bulletList",
    "description": "Tour options with different pax-based pricing and child cost reduction"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Option name (e.g., Standard, Premium, Budget)"
    },
    "description": {
      "type": "string"
    },
    "paxRates": {
      "displayName": "paxRates",
      "type": "component",
      "repeatable": true,
      "component": "experience.pax-rates",
      "required": true
    },
    "childCostReduction": {
      "type": "component",
      "repeatable": false,
      "component": "experience.rates",
      "required": true,
      "description": "Amount to subtract from adult price for children (USD and LKR)"
    }
  }
}
